pseudoOrder <- function(object,times){
  ## obsstatus <- object$model.response[,"status"]
  ## obstimes <- object$model.response[,"time"][obsstatus!=0]
  ## surv <- prodlim::predict(object,times=otimes,type="surv")
  ## cuminc <- 1-surv
  ## loo.surv <- leaveOneOut
  ## stopifnot(length(times)<2)
  ## lapply(times,function(t){
  ## sum(n*n*surv * diff(cuminc))-
  ## }
}
